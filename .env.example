# Public, build-time env vars (inlined into the static output — no secrets here).
# Base URL of the Ship API that the waitlist form posts to. Empty disables submit.
PUBLIC_SHIP_API_URL=https://app.shipplatform.ai
# PostHog project key. Empty disables analytics (idle-loaded only when set).
PUBLIC_POSTHOG_KEY=
