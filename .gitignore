# dependencies
/node_modules

# astro build output
/dist/
/.astro/
# NOTE: /docs/ is the committed GitHub Pages publish dir (produced by `pnpm deploy`) — not ignored.

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (but keep the example)
.env*
!.env.example

# typescript
*.tsbuildinfo

# editors
.idea
